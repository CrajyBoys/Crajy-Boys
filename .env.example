# Copy to .env and edit
PORT=4000
BASE_URL=http://localhost:4000
# FRONTEND_AFTER_VERIFY optional (redirect after user clicks verification link)
#FRONTEND_AFTER_VERIFY=http://localhost:5500/members.html

# SMTP: put real values for production (SendGrid/Mailgun/SES). If empty, server uses Ethereal for dev.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

EMAIL_FROM=No Reply <no-reply@crajy-boys.local>
BCRYPT_ROUNDS=12
# Optional: DB_PATH=./crajy_members.db